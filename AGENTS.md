# AGENTS.md

This file points agents to the repository guidance in `CLAUDE.md` and `.claude/settings.json`.

## Imported Guidance

- Read `CLAUDE.md` first — project overview, commands, architecture, testing conventions, stack details, CI, and hooks.
- Read `.claude/settings.json` before editing API docs. It configures worktree isolation and runs `node scripts/sync-api-docs.mjs` whenever `openapi.yaml` is edited.
